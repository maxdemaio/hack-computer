/** Hello World program. */
class Main {
  function void main() {
    // idea: have an array of bats
    // when you touch one you call the dealloc
    // 
    /* Prints some text using the standard library. */
    do Output.printString("Hello World");
    do Output.println(); // New line
    
    var BatCatcherGame game;
    let game = BatCatcherGame.new();
    do game.run();
    do game.dispose();
    return;
  }
}