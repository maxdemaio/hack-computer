class Random {
  field int seed;

  constructor Random new(int seedIn) {
    let seed = seedIn;
    return this;
  }

  method int randInt(int range) {
    // Linear Congruential Generator (LCG)
    let seed = (seed * 1103515245 + 12345) & 0x7FFFFFFF;
    return seed % range;
  }
}
